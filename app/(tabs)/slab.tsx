import { Text, View } from 'react-native';

export default function Slab() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ color: '#000' }}>Slab Screen</Text>
    </View>
  );
}
